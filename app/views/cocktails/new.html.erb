<div class="container">
  <h1>Add a Puptail</h1>
  <div class="add_form">
  <%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Finish! 👌🏽", class: "btn btn-outline-light" %>
  <% end %>
  </div>

</div>
